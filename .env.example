# 复制本文件为 .env 后填入实际值。.env 已经被 .gitignore，永远不会进入仓库。

# ============================================================
# 必填：vault 绝对路径
# ============================================================
# Windows 示例: VAULT_PATH=C:\Users\you\Documents\SecondBrain
# macOS / Linux 示例: VAULT_PATH=/home/you/SecondBrain
VAULT_PATH=

# ============================================================
# LLM 提供方：remote 或 ollama
# ============================================================
LLM_PROVIDER=remote

# ============================================================
# 远程 LLM（OpenAI 兼容接口；OpenAI / 智谱 / DeepSeek / NVIDIA NIM / Kimi 等都可）
# ============================================================
REMOTE_API_KEY=
REMOTE_BASE_URL=https://api.openai.com/v1
REMOTE_MODEL_NAME=gpt-4o-mini

# 单次请求超时秒数
REMOTE_TIMEOUT=60

# 仅对超时 / 5xx / 429 / 408 等可重试错误生效，指数退避
REMOTE_MAX_RETRIES=3

# ============================================================
# 本地 Ollama（仅在 LLM_PROVIDER=ollama 时使用）
# ============================================================
OLLAMA_MODEL_NAME=qwen3:4b

# ============================================================
# 采样参数（remote 和 ollama 都生效；不写就用代码里的默认值）
# ============================================================
# 越低越稳定。知识抽取建议 0.1~0.3。
# 注意：智谱 GLM 要求 > 0，不要写 0.0。
LLM_TEMPERATURE=0.2

# nucleus sampling，与 temperature 并存。
LLM_TOP_P=0.9

# 输出 token 上限，防止跑飞。中文知识整理一般 800~1500 够；觉得被截断就调到 2000~3000。
LLM_MAX_TOKENS=1500

# ============================================================
# 日志级别（可选，默认 INFO）
# ============================================================
# 可选值：DEBUG / INFO / WARNING / ERROR
# DEBUG 会打印 watcher 跳过已处理文件、文件稳定检测过程等细节，适合排查问题时临时开启。
# LOG_LEVEL=DEBUG
